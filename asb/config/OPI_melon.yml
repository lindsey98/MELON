injection_method: observation_prompt_injection
attack_tool:
  - all
  # - agg
  # - non-agg

llms:
  - gpt-4o-mini
  # - gpt-4o-2024-08-06
  # - claude-3-5-sonnet-20240620
  # - ollama/qwen2:72b
  # - ollama/llama3.1:8b

attack_types:
  # - combined_attack
  - context_ignoring
  # - fake_completion
  # - escape_characters
  # - naive

# MELON contrastive prompt-injection defense (see pyopenagi/agents/melon.py).
# Configure the detection-embedding backend via MELON_EMBED_* env vars, e.g.
#   MELON_EMBED_PROVIDER=openai            (default; needs OPENAI_API_KEY)
#   MELON_EMBED_PROVIDER=sentence-transformers   (fully local, no API)
defense_type: melon
suffix: melon
